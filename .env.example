# Fill in real values per Phase 0 / "Read this first" in gigabit-lora-demo-plan.md.
# No defaults in code — missing vars must fail loudly.
#
# Same on both nodes:
MASTER_ADDR=192.168.50.1
MASTER_PORT=29500
WORLD_SIZE=2

# Per machine:
RANK=0
NCCL_SOCKET_IFNAME=